package com.dyhx.kdtask.db.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.dyhx.kdtask.db.model.MinuteTaskMd;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "MINUTE_TASK_MD".
*/
public class MinuteTaskMdDao extends AbstractDao<MinuteTaskMd, Long> {

    public static final String TABLENAME = "MINUTE_TASK_MD";

    /**
     * Properties of entity MinuteTaskMd.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Day = new Property(1, int.class, "day", false, "DAY");
        public final static Property Hour = new Property(2, int.class, "hour", false, "HOUR");
        public final static Property Minute = new Property(3, int.class, "minute", false, "MINUTE");
        public final static Property Name = new Property(4, String.class, "name", false, "NAME");
        public final static Property StartTime = new Property(5, String.class, "startTime", false, "START_TIME");
        public final static Property EndTime = new Property(6, String.class, "endTime", false, "END_TIME");
        public final static Property Describe = new Property(7, String.class, "describe", false, "DESCRIBE");
    }


    public MinuteTaskMdDao(DaoConfig config) {
        super(config);
    }
    
    public MinuteTaskMdDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MINUTE_TASK_MD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"DAY\" INTEGER NOT NULL ," + // 1: day
                "\"HOUR\" INTEGER NOT NULL ," + // 2: hour
                "\"MINUTE\" INTEGER NOT NULL ," + // 3: minute
                "\"NAME\" TEXT," + // 4: name
                "\"START_TIME\" TEXT," + // 5: startTime
                "\"END_TIME\" TEXT," + // 6: endTime
                "\"DESCRIBE\" TEXT);"); // 7: describe
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MINUTE_TASK_MD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, MinuteTaskMd entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getDay());
        stmt.bindLong(3, entity.getHour());
        stmt.bindLong(4, entity.getMinute());
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(5, name);
        }
 
        String startTime = entity.getStartTime();
        if (startTime != null) {
            stmt.bindString(6, startTime);
        }
 
        String endTime = entity.getEndTime();
        if (endTime != null) {
            stmt.bindString(7, endTime);
        }
 
        String describe = entity.getDescribe();
        if (describe != null) {
            stmt.bindString(8, describe);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, MinuteTaskMd entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getDay());
        stmt.bindLong(3, entity.getHour());
        stmt.bindLong(4, entity.getMinute());
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(5, name);
        }
 
        String startTime = entity.getStartTime();
        if (startTime != null) {
            stmt.bindString(6, startTime);
        }
 
        String endTime = entity.getEndTime();
        if (endTime != null) {
            stmt.bindString(7, endTime);
        }
 
        String describe = entity.getDescribe();
        if (describe != null) {
            stmt.bindString(8, describe);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public MinuteTaskMd readEntity(Cursor cursor, int offset) {
        MinuteTaskMd entity = new MinuteTaskMd( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // day
            cursor.getInt(offset + 2), // hour
            cursor.getInt(offset + 3), // minute
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // name
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // startTime
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // endTime
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // describe
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, MinuteTaskMd entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDay(cursor.getInt(offset + 1));
        entity.setHour(cursor.getInt(offset + 2));
        entity.setMinute(cursor.getInt(offset + 3));
        entity.setName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setStartTime(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setEndTime(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setDescribe(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(MinuteTaskMd entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(MinuteTaskMd entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(MinuteTaskMd entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
